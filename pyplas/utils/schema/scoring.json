{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Scoring Validate",
    "type": "object",
    "properties": {
        "p_id": {"type": "string"},
        "q_id": {"type": "string"},
        "ptype": {
            "type": "integer",
            "enum": [0, 1]
        },
        "answers": {
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "job_id": {"type": "string"}
    },
    "required": ["p_id", "q_id", "ptype", "answers", "job_id"],
    "additionalProperties": false
}